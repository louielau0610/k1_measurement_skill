# Unitree G1 Platform Scaffold

This package is a planning scaffold only. It defines where a future Unitree G1 command adapter, state logger, and extractor should live, but it does not access hardware or synthesize measurements.

`hardware_validated_reference` is `false` until real G1 logs and validation evidence are added.
