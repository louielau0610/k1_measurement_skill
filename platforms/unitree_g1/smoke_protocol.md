# Unitree G1 Smoke Protocol Placeholder

Before enabling Unitree G1 calibration, add a real state source, a read-only logging smoke test, and an extractor that proves position and yaw are populated from robot logs.

This file is not evidence of Unitree G1 validation.
