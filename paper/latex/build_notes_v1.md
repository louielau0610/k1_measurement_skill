# LaTeX Build Notes v1
> Non-final scaffold (P16). No PDF built. No submission package.

## Build commands (when tools available)
```
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

## P17 Status
Tables 1-3 converted to LaTeX (.tex files). Figures not rendered (Mermaid CLI unavailable). Section files updated to input table files.

## Current blockers
- Figures not rendered (SVG pending mmdc/mermaid.live).
- Tables not converted to LaTeX format (pending P17).
- Final title and author not selected.
- Venue/template not selected.
- Scientific evidence gaps remain (no navigation outcomes, no held-out eval).

## Build status: NOT BUILT — NON-FINAL SCAFFOLD ONLY
