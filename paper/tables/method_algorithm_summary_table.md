# Method Algorithm Summary Table

| algorithm | purpose | input | output | key_conservative_rule | unsupported_operation_avoided | source_file_or_artifact |
| --- | --- | --- | --- | --- | --- | --- |
| Algorithm 1: Measurement artifacts to velocity-response dataset | Construct a schema-governed dataset from real K1 measurement records. | Measurement v0 artifacts, schema v1 | `velocity_response_dataset_v1.json` + validation report | Omit unavailable fields rather than fabricating values. Preserve qualitative labels when numeric response is absent. | No fabrication of `vx_actual_mps_mean` for qualitative-only records. No imputation of absent fields. | `scripts/build_velocity_response_dataset_v1.py`, `k1_measurement/velocity_response_dataset_builder.py` |
| Algorithm 2: Conservative response prediction | Produce velocity response predictions with uncertainty labels from sparse dataset evidence. | Dataset v1, query `vx_cmd` | `VelocityResponsePrediction` | Exact-match qualitative-only records return qualitative labels without numeric fabrication. Out-of-range queries return conservative uncertainty. | No interpolation across a qualitative-only gap. No extrapolation beyond evaluated command range. No calibrated probability output. | `scripts/run_velocity_response_model_v1.py`, `k1_measurement/velocity_response_model.py` |
| Algorithm 3: Response prediction to navigation-risk assessment | Translate response predictions into advisory navigation-risk metadata. | `VelocityResponsePrediction` | `NavigationRiskAssessment` | Risk categories are advisory only. All outputs carry allowed/disallowed downstream use lists. | No automatic compensation triggering. No inverse command computation. No robot motion commands issued. | `scripts/run_navigation_risk_mapping_v1.py`, `k1_measurement/navigation_risk_mapping.py` |
| Algorithm 4 (optional): Claim-governed evaluation | Consolidate pipeline artifacts into an evaluation package that separates structural from performance evidence. | All upstream outputs, claim registry, evidence table, non-claims | `m17_pipeline_evaluation_report.json` + artifact table | Explicitly list unavailable metrics (collision, near-miss, success rate) as missing, not as zero or absent. | No inference of navigation outcomes from structural artifacts. No upgrade of candidate contributions to final claims. | `scripts/generate_research_pipeline_evaluation_v1.py`, `k1_measurement/research_pipeline_evaluation.py` |
